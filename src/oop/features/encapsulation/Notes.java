package oop.features.encapsulation;

public class Notes {
    // Encapsulation is used for data protection
    // It mostly deals with accessing of data to particular ones
    // We use access modifiers (public, private, protected, default)
    // Package: group of classes, interfaces, enums, exceptions, errors which are working for a similar functionality.
    // Packages for entities, models, services, repos, utilities, etc.,
    //
}
